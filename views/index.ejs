<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pokédex</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <link href="styles.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <div class="form-container">
            <h1 class="text-center">Pokédex</h1>
            <form action="/submit" method="POST" class="d-flex">
                <input type="text" name="message" class="form-control me-2" placeholder="Enter Pokémon name" required>
                <button type="submit" class="btn btn-danger">Search</button>
            </form>
        </div>

        <% if (locals.pokemon) { %>
            <div class="card pokemon-card">
                <div class="card-header text-center">
                    <%= pokemon.name %>
                </div>
                <div class="card-body text-center">
                    <img src="<%= pokemon.image %>" class="img-fluid" alt="<%= pokemon.name %>">
                    <h5 class="card-title mt-3"><%= pokemon.name %></h5>
                    <div class="row">
                        <div class="col-6">
                            <h6>Abilities</h6>
                            <ul class="list-group">
                                <% pokemon.abilities.forEach(ability => { %>
                                    <li class="list-group-item"><%= ability %></li>
                                <% }); %>
                            </ul>
                        </div>
                        <div class="col-6">
                            <h6>Types</h6>
                            <ul class="list-group">
                                <% pokemon.types.forEach(type => { %>
                                    <li class="list-group-item"><%= type %></li>
                                <% }); %>
                            </ul>
                        </div>
                    </div>
                    <h6 class="mt-3">Stats</h6>
                    <ul class="list-group">
                        <% pokemon.stats.forEach(stat => { %>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <%= stat.name %>
                                <span class="badge bg-danger rounded-pill"><%= stat.base_stat %></span>
                            </li>
                        <% }); %>
                    </ul>
                </div>
            </div>
        <% } else { %>
            <p class="text-center">Search for a Pokémon to see its card.</p>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
